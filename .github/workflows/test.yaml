name: Perl CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4.2.2

      - name: Set up Perl
        uses: shogo82148/actions-setup-perl@v1.33.0
        with:
          perl-version: '5.36'

      - name: Install dependencies
        run: |
          dpkg -L libipc-run-perl | grep Run.pm
          echo "=========="
          sudo apt-get update
          sudo apt-get install -y librrds-perl \
          libjson-perl \
          libhtml-parser-perl \
          libcgi-pm-perl \
          libipc-run-perl
          sudo cpan IPC::Run

      - name: Run tests
        run: |
          prove -lrv t
